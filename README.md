# dotfiles

zsh + antidote + starship + mise, in Ghostty.

```sh
git clone https://github.com/cdotta/dotfiles ~/Projects/own/dotfiles
~/Projects/own/dotfiles/install.sh
```

Then import the GPG key and open Ghostty.
